#include <ActualizacionOta.h>

ActualizacionOta *ActualizacionOta::instance_OTA;

//ActualizacionOta::ActualizacionOta(Interruptor luz, Interruptor led_testigo);

/*
ActualizacionOta *ActualizacionOta::begin(){
    if(instance_OTA == NULL){
       // instance_OTA = new ActualizacionOta(luz,led_testigo);
    } 
}
*/
